<table>
  <tr>
    <th>Тема</th>
    <th>Текст заявки</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @helps.each do |help| %>
  <tr>
    <td><%= help.title %></td>
    <td><%= help.body %></td>
    <td><%= link_to 'Показать', help %></td>
    <td><%= link_to 'Редактироваь', edit_help_path(help) %></td>
    <td><%= link_to 'Удалить', help, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить новую заявку', new_help_path %>
