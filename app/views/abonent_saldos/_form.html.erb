<%= form_tag("", :method => "get") do %>

  <%= label_tag(:q, "Корпорация ") %>
  <%= collection_select :corporation, :corporation_id, @corporations, :id, :name, :include_blank => false %><br>
  <%= label_tag(:abonent_id, "Абонент") %>
  <%= grouped_collection_select(:corporation, :abonent_id, @corporations, :abonents, :name, :id, :phone, :inlcude_blank => true )%><br>
  <%= label_tag(:q, "С ") %>
  <%= text_field_tag 'from_day', nil, :class => 'datepicker' %><br>
  <%= label_tag(:q, "До ") %> 
  <%= text_field_tag 'to_day', nil, :class => 'datepicker' %>
  <%= submit_tag "Сформировать" %>

<% end %>
